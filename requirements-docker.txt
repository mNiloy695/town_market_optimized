# Core Django
Django==6.0.3
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1

# Database
dj-database-url==2.2.0
psycopg2-binary==2.9.9

# Redis for caching and message broker
redis==5.0.8
channels-redis==4.2.1

# Channels (WebSocket/chat) + Daphne (ASGI server for production)
channels==4.3.2
daphne==4.2.1

# Configuration
python-decouple==3.8

# Phone number field
django-phonenumber-field==8.4.0

# Filtering
django-filter==25.2

# Essential dependencies
asgiref==3.11.1
pytz==2024.1
sqlparse==0.5.0
six==1.16.0
django-cors-headers==4.7.0

# Cloudflare R2 Storage (S3-compatible backend)
django-storages==1.14.6
boto3>=1.34.0

# ASGI server for production (used by docker-compose.prod.yml as of the
# Daphne switch); gunicorn retained for WSGI fallback.
daphne==4.2.1
gunicorn==22.0.0
