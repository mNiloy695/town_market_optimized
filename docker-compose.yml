services:
  # Django Application
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - .env
    depends_on:
      - db
      - redis
    networks:
      - townmarket

  # Celery Worker
  celery_worker:
    build: .
    command: celery -A core worker --loglevel=info
    volumes:
      - .:/app
    env_file:
      - .env
    restart: always
    depends_on:
      - db
      - redis
    networks:
      - townmarket

  # Celery Beat (Scheduler)
  celery_beat:
    build: .
    command: celery -A core beat --loglevel=info --schedule /tmp/celerybeat-schedule
    volumes:
      - .:/app
    env_file:
      - .env
    restart: always
    depends_on:
      - db
      - redis
    networks:
      - townmarket

  # Redis
  redis:
    image: redis:7-alpine
    ports:
      - "6389:6379"
    volumes:
      - redis_data:/data
    networks:
      - townmarket
    restart: always
    env_file:
      - .env

  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5430:5432"
    networks:
      - townmarket
    restart: always
    env_file:
      - .env
  ngrok:
    image: ngrok/ngrok:latest
    command: http --url=https://outmatch-marvelous-mummify.ngrok-free.dev --pooling-enabled web:8000
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    ports:
      - "4041:4040"
    restart: always
    depends_on:
      - web
    networks:
      - townmarket

volumes:
  postgres_data:
  redis_data:
  static_volume:
  media_volume:

networks:
  townmarket:
    driver: bridge
